[project]
name = "wiki-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "flask-caching>=2.3",
    "markdown-it-py>=3.0",
    "mdit-py-plugins>=0.4",
    "playwright>=1.40",
    "pdfplumber>=0.10",
    "pillow>=10.0",
    "yt-dlp>=2024.1",
    "graphviz>=0.20",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-flask>=1.3",
    "ruff>=0.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["wiki_server"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
