version: "3.9"

services:
  wiki:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./wiki:/app/wiki
      - ./raw:/app/raw
      - ./search.db:/app/search.db
      - ./access.log:/app/access.log
    restart: unless-stopped
