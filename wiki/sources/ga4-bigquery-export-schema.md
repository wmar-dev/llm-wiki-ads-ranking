---
title: "GA4 BigQuery Export Schema (Analytics Help)"
type: "source_summary"
sources:
  - "web/ga4-bigquery-export-schema.md"
source_url: "https://support.google.com/analytics/answer/7029846"
status: "current"
source_type: "official_documentation"
processing_status: "processed"
ingest_count: 1
created: "2026-06-10"
last_updated: "2026-06-10"
---

# GA4 BigQuery Export Schema (Analytics Help)

**Source type:** `official_documentation`

## Summary

GA4 properties linked to BigQuery export raw event data into a per-property
dataset (`analytics_<property_id>`), with daily (`events_YYYYMMDD`) and optional
intraday (`events_intraday_YYYYMMDD`) tables. Each event row carries repeatable
`event_params`, `user_properties`, and (for ecommerce) `items` records. GA4 also
maintains user-level tables with audience memberships, lifetime-value metrics, and
**predictive scores** (purchase-probability, churn-probability) generated by GA4's
built-in ML models; BigQuery ML can build further custom predictive audiences on
top of this data. Joining `collected_traffic_source.gclid` from GA4 events to
Google Ads' click-transfer tables connects on-site conversions back to the
specific ad click that drove them [[raw/web/ga4-bigquery-export-schema.md]].

## Key Claims

| Claim | Status |
|---|---|
| GA4 exports raw, row-per-event data to BigQuery (daily + intraday tables) | Stated |
| Events carry repeatable event_params/user_properties/items records | Stated |
| GA4 computes predictive scores (purchase/churn probability) per user | Stated (from search aggregation, not the primary fetched doc) |
| GA4-to-Google-Ads join uses `gclid` to attribute conversions to ad clicks | Stated (from search aggregation) |
| Intraday/streaming export tables (`events_intraday_YYYYMMDD`) populate within minutes of an event; the finalized daily table (`events_YYYYMMDD`) is updated for up to ~3 days afterward | Stated — confirmed by Google's "BigQuery Export" overview (support.google.com/analytics/answer/9358801), which describes intraday data as available "within a few minutes" |

**Note:** this BigQuery export path (minutes-to-3-days) is the latency for raw
analytics/BQML access. It is a *different pipeline* from GA4's "key event" export
into Google Ads conversion actions, which is what feeds Smart Bidding — see
[[wiki/sources/ga4-conversions-google-ads-data-minefield.md]] for that path's
6–18 hour latency.

## Relevance to Ads Ranking

This is the technical mechanism behind **conversion attribution** described in
[[wiki/synthesis/what-is-a-conversion.md]]: the `gclid` join is exactly how a
GA4-tracked on-site conversion gets matched back to the ad click that drove it,
making it eligible as a training label for pCVR models and as input to Smart
Bidding [[wiki/concepts/google-ad-rank-ltv-scoring.md]].

## Related Pages

- [[wiki/synthesis/how-does-google-analytics-work.md]]
- [[wiki/sources/google-analytics-data-collection.md]]
- [[wiki/synthesis/what-is-a-conversion.md]]
